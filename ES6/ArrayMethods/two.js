let enames = ["Rahul", "Sonia", "Priya", "Modi", "DK Shiva"]

enames.map((name, i) => {
    console.log(name, i)
})
/* enames.map(function (name) {
    console.log(name)
}) */
/* for (let i = 0; i <= enames.length - 1; i++) {
    console.log(enames[i])
} */
/* 
for (ename of enames) {
    console.log(ename)
} */