let arr = []
let size = ['S', 'M', 'L']

size.length > 0 ? console.log("Not Empty Array") : console.log("Empty Array")

/* if (size.length > 0) {
    console.log(" Not Empty Array")
}
else {
    console.log("Empty Array")
} */